FROM golang:latest

# Установка рабочей директории внутри контейнера
WORKDIR /app

# Копирование файлов проекта внутрь контейнера
COPY . .

# Сборка исполняемого файла
RUN go build -o main .

# Запуск программы по умолчанию с передачей аргумента входного файла
CMD ["./main", "$INPUT_FILE"]